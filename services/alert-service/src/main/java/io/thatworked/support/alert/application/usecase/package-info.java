/**
 * Use cases implementing specific business operations.
 * 
 * Each use case represents a single business transaction and:
 * - Accepts only primitive parameters (no DTOs)
 * - Returns domain objects
 * - Orchestrates domain services and repositories
 * - Publishes domain events
 */
package io.thatworked.support.alert.application.usecase;