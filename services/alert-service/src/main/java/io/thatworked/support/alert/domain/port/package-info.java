/**
 * Domain ports defining contracts for external dependencies.
 * These interfaces allow the domain to remain independent of infrastructure concerns.
 */
package io.thatworked.support.alert.domain.port;