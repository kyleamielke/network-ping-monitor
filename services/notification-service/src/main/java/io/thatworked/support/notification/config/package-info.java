/**
 * Configuration package for notification service.
 * 
 * This package contains Spring configuration classes for setting up
 * infrastructure components such as:
 * - Logging configuration with StructuredLoggerFactory
 * - Kafka producer/consumer configuration
 * - Jackson ObjectMapper configuration
 * - Database configuration
 * 
 * These configuration classes are part of the infrastructure layer
 * and wire up the technical components needed by the application.
 */
package io.thatworked.support.notification.config;