/**
 * Domain ports defining contracts for external dependencies.
 * 
 * These interfaces define the boundaries of the domain:
 * - NotificationRepository: Port for notification persistence
 * - NotificationSender: Port for sending notifications through channels
 * - EventPublisher: Port for publishing domain events
 * - DomainLogger: Port for business-oriented logging
 */
package io.thatworked.support.notification.domain.port;