/**
 * Domain services containing core business logic.
 * 
 * Domain services:
 * - Orchestrate complex business operations
 * - Enforce business rules and invariants
 * - Handle cross-aggregate transactions
 * - Contain logic that doesn't naturally fit in entities
 * 
 * No infrastructure dependencies allowed.
 */
package io.thatworked.support.device.domain.service;