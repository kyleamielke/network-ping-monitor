package io.thatworked.support.search.application.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration for the application layer.
 * Application-specific beans and configurations go here.
 */
@Configuration
public class ApplicationConfig {
    // Application layer specific configurations
    // Domain services are configured in infrastructure.config.DomainConfiguration
}