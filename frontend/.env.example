# Frontend Environment Variables

# API Gateway Configuration (for Docker deployment)
# These are used by the nginx proxy in the container
API_GATEWAY_HOST=api-gateway
API_GATEWAY_PORT=8080

# Development API URLs (for local development with npm run dev)
# Update these with your server's IP address if needed
# Example: VITE_API_URL=http://192.168.1.100:8080
VITE_API_URL=http://localhost:8080
VITE_WS_URL=ws://localhost:8080

# Development settings
VITE_NODE_ENV=development